localhost {
    root .
    log stdout

    proxy / localhost:8888 {
        transparent
        websocket
    }

    proxy /api localhost:8444 {
        transparent
        websocket
        without /api
    }
}
